# Drift (PRD §18): nama tabel & kolom lokal sama dengan server (PascalCase), tidak diubah ke snake_case.
targets:
  $default:
    builders:
      drift_dev:
        options:
          case_from_dart_to_sql: preserve
          store_date_time_values_as_text: true
