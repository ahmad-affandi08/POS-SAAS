name: kasir
description: "Aplikasi POS: kasir, KDS, dan gudang (Android, iOS/iPadOS, Windows)."
publish_to: none
version: 0.1.0+1
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  inti:
    path: ../../Paket/Inti
  sistem_desain:
    path: ../../Paket/SistemDesain

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
