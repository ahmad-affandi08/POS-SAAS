name: inti
description: Tipe dasar bersama (Uang, Kuantitas, ModePembulatan). Dart murni, tanpa Flutter.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  decimal: ^3.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
